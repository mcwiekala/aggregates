package io.cwiekala.agregates.domain.auction;

import io.cwiekala.agregates.utils.Entity;

@Entity
class ManualBid {
    // maxiumum amount
    // auctionerId
    // auctioner can bid over his own maximum, but the price shouldnt be changed
}
